s is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Your Name <youremail@domain.com>
pkgname=suckless-calamares
pkgver=1.0.0
pkgrel=1
epoch=
pkgdesc="Installer for Suckless Linux"
arch=(x86_64)
url=""
license=('GPL')
groups=()
depends=()
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
#source=("~/suckless-calamares")
noextract=()
md5sums=()
validpgpkeys=()
pkgdir=("~/suckless-calamares/repo/x86_64")

prepare() {
	cd ~/suckless-calamares
	mkdir -v build
}

build() {
	cd ~/suckless-calamares
	cmake -DCMAKE_BUILD_TYPE=Debug -B build
	cd build 
	make
	cd ..
	cp -rv ~/suckless-calamares/src/branding/suckless/* ~/suckless-calamares/build/src/branding/suckless
	cp -v ~/suckless-calamares/settings.conf ~/suckless-calamares/build/settings.conf
	
}

check() {
	#cd "$pkgname-$pkgver"
	#make -k check
	echo "Skipping check()"
}

package() {
	cd "~/suckless-calamares"
	make DESTDIR="$pkgdir/" install
}

